export type SiteMetadata = {
  title: string
  description: string
  siteUrl: string
  author: {
    name: string
    summary: string
    url: string
  }
  social: {
    twitter: string
    github: string
  }
  repo: string
}
